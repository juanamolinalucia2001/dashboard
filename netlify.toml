# Dashboard estático: datos embebidos en el build (dashboard_data.json)
[build]
  command = "cd dashboard-ui && npm ci && npm run build"
  publish = "dashboard-ui/dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
