# Sitio estático: dashboard.html (datos embebidos, sin API)
[build]
  command = "mkdir -p netlify-publish && cp dashboard.html netlify-publish/index.html"
  publish = "netlify-publish"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
